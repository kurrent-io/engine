{
  "name": "@kurrent/phaselock-typespec-tests",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Test suite for the PhaseLock TypeSpec tooling",
  "scripts": {
    "gen": "tsp compile fixtures",
    "test": "vitest run",
    "test:py": "python3 py/test_checkers.py",
    "test:go": "cp 'tsp-output/@kurrent/phaselock-typespec-go/model.go' go/model.go && cd go && go test ./...",
    "watch": "vitest"
  },
  "dependencies": {
    "@kurrent/phaselock-typespec": "workspace:*",
    "@kurrent/phaselock-typespec-ts": "workspace:*",
    "@kurrent/phaselock-typespec-py": "workspace:*",
    "@kurrent/phaselock-typespec-go": "workspace:*",
    "@typespec/compiler": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "vitest": "^2.1.0"
  }
}
